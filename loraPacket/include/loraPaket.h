#pragma once#include <cstring>#include <cstdint>#include <cstdio>#include <cstdlib>#include <memory>class LoRaPacket {private:  uint8_t *_payload;  size_t _payload_len;  int _packet_rssi;  int _freq_err;  float _snr;  void _setPayload(unsigned char *, size_t);public:  LoRaPacket(uint8_t *payload, size_t size);  LoRaPacket(uint8_t *payload, size_t size, int, int, float);  ~LoRaPacket();  unsigned char *getPayload();  size_t payloadLength();  int getPacketRSSI();  int getFreqErr();  float getSNR();  void setPayload(unsigned char *, size_t size);};